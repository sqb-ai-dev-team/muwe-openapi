# OpenAPI Contract

The machine-readable API contract lives at:

```text
openapi/openapi.yaml
```

The contract is the source for SDK generation, partner review, and automated compatibility checks. Human documentation may explain behavior, but endpoint fields and response structures should be kept synchronized with the OpenAPI file.
